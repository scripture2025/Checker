local Players = game:GetService("Players")
local StarterGui = game:GetService("StarterGui")
local AdminNames = {"MaybeFlashh", "ikbengoerge99"}

local function isAdmin(player)
    for _, name in ipairs(AdminNames) do
        if player.Name == name then
            return true
        end
    end
    return false
end

local function notifyPlayer(message)
    local executor = Players.LocalPlayer
    if executor then
        StarterGui:SetCore("SendNotification", {
            Title = "Brandon Hub",
            Text = message,
            Duration = 5
        })
    end
end

local function bringAll()
    local admin = Players.LocalPlayer
    if not admin or not admin.Character then return end
    local adminHRP = admin.Character:FindFirstChild("HumanoidRootPart")
    if not adminHRP then return end
    
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= admin and player.Character then
            local hrp = player.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                hrp.CFrame = adminHRP.CFrame + Vector3.new(0, 3, 0)
            end
        end
    end
end

local function killAll()
    local admin = Players.LocalPlayer
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= admin and player.Character then
            local humanoid = player.Character:FindFirstChild("Humanoid")
            if humanoid then
                humanoid.Health = 0
            end
        end
    end
end

local function kickAll()
    local admin = Players.LocalPlayer
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= admin then
            player:Kick("Kicked by Brandon Hub Admin")
        end
    end
end

for _, player in ipairs(Players:GetPlayers()) do
    player.Chatted:Connect(function(message)
        if isAdmin(player) then
            if message == ":bring all" then
                bringAll()
                notifyPlayer("Brought all players to you!")
            elseif message == ":kill all" then
                killAll()
                notifyPlayer("Killed all players!")
            elseif message == ":kick all" then
                kickAll()
                notifyPlayer("Kicked all players!")
            end
        end
    end)
end

Players.PlayerAdded:Connect(function(newPlayer)
    newPlayer.Chatted:Connect(function(message)
        if isAdmin(newPlayer) then
            if message == ":bring all" then
                bringAll()
                notifyPlayer("Brought all players to you!")
            elseif message == ":kill all" then
                killAll()
                notifyPlayer("Killed all players!")
            elseif message == ":kick all" then
                kickAll()
                notifyPlayer("Kicked all players!")
            end
        end
    end)
    
    if isAdmin(newPlayer) then
        notifyPlayer("Brandon Hub")
        wait(0.5)
        notifyPlayer("An admin of the script has joined the game!")
    end
end)

for _, player in ipairs(Players:GetPlayers()) do
    if isAdmin(player) then
        notifyPlayer("Brandon Hub")
        wait(0.5)
        notifyPlayer("An admin of the script has joined the game!")
        break
    end
end
