local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local TextChatService = cloneref(game:GetService("TextChatService"))
local CoreGui = game:GetService("CoreGui")

local isLegacyChat = TextChatService.ChatVersion == Enum.ChatVersion.LegacyChatService

local function SendMessage(message)
    message = tostring(message)
    if not isLegacyChat then
        TextChatService.TextChannels.RBXGeneral:SendAsync(message)
    else
        ReplicatedStorage.DefaultChatSystemChatEvents.SayMessageRequest:FireServer(message, "All")
    end
end

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "BrandonFakeLoggerV2"
screenGui.Parent = CoreGui

local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 250, 0, 170)
mainFrame.Position = UDim2.new(0.5, -125, 0.5, -85)
mainFrame.BackgroundColor3 = Color3.fromRGB(40, 25, 10)
mainFrame.BackgroundTransparency = 0.05
mainFrame.BorderSizePixel = 0
mainFrame.ClipsDescendants = true
mainFrame.Parent = screenGui

local shadow = Instance.new("ImageLabel")
shadow.Size = UDim2.new(1, 30, 1, 30)
shadow.Position = UDim2.new(0, -15, 0, -15)
shadow.BackgroundTransparency = 1
shadow.Image = "rbxassetid://6015897842"
shadow.ImageTransparency = 0.2
shadow.ScaleType = Enum.ScaleType.Slice
shadow.SliceCenter = Rect.new(10, 10, 10, 10)
shadow.Parent = mainFrame

local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 8)
mainCorner.Parent = mainFrame

local topBar = Instance.new("Frame")
topBar.Size = UDim2.new(1, 0, 0, 28)
topBar.Position = UDim2.new(0, 0, 0, 0)
topBar.BackgroundColor3 = Color3.fromRGB(200, 100, 20)
topBar.BackgroundTransparency = 0.15
topBar.Parent = mainFrame

local topCorner = Instance.new("UICorner")
topCorner.CornerRadius = UDim.new(0, 8)
topCorner.Parent = topBar

local title = Instance.new("TextLabel")
title.Size = UDim2.new(0.8, 0, 1, 0)
title.Position = UDim2.new(0.05, 0, 0, 0)
title.BackgroundTransparency = 1
title.Text = "⚡ Brandon Logger V2"
title.TextColor3 = Color3.fromRGB(255, 180, 80)
title.TextSize = 13
title.Font = Enum.Font.GothamBold
title.TextXAlignment = Enum.TextXAlignment.Left
title.TextYAlignment = Enum.TextYAlignment.Center
title.Parent = topBar

local closeButton = Instance.new("TextButton")
closeButton.Size = UDim2.new(0, 22, 0, 22)
closeButton.Position = UDim2.new(1, -26, 0, 3)
closeButton.BackgroundColor3 = Color3.fromRGB(180, 40, 40)
closeButton.BackgroundTransparency = 0.3
closeButton.Text = "✕"
closeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
closeButton.TextSize = 14
closeButton.Font = Enum.Font.GothamBold
closeButton.Parent = topBar

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(0, 4)
closeCorner.Parent = closeButton

closeButton.MouseButton1Click:Connect(function()
    screenGui.Enabled = false
end)

local dragBar = Instance.new("Frame")
dragBar.Size = UDim2.new(0.8, 0, 1, 0)
dragBar.Position = UDim2.new(0.05, 0, 0, 0)
dragBar.BackgroundTransparency = 1
dragBar.Parent = topBar

local inputContainer = Instance.new("Frame")
inputContainer.Size = UDim2.new(0.7, -10, 0, 28)
inputContainer.Position = UDim2.new(0.05, 0, 0, 38)
inputContainer.BackgroundColor3 = Color3.fromRGB(30, 20, 10)
inputContainer.BackgroundTransparency = 0.3
inputContainer.Parent = mainFrame

local inputCorner = Instance.new("UICorner")
inputCorner.CornerRadius = UDim.new(0, 4)
inputCorner.Parent = inputContainer

local inputBox = Instance.new("TextBox")
inputBox.Size = UDim2.new(1, -8, 1, 0)
inputBox.Position = UDim2.new(0, 4, 0, 0)
inputBox.BackgroundTransparency = 1
inputBox.TextColor3 = Color3.fromRGB(255, 200, 150)
inputBox.TextSize = 12
inputBox.Font = Enum.Font.Gotham
inputBox.PlaceholderText = "🔍 Enter display name..."
inputBox.PlaceholderColor3 = Color3.fromRGB(150, 120, 80)
inputBox.Text = ""
inputBox.ClearTextOnFocus = false
inputBox.TextXAlignment = Enum.TextXAlignment.Left
inputBox.Parent = inputContainer

local displayNameResult = Instance.new("TextLabel")
displayNameResult.Size = UDim2.new(0.7, -10, 0, 20)
displayNameResult.Position = UDim2.new(0.05, 0, 0, 70)
displayNameResult.BackgroundTransparency = 1
displayNameResult.Text = ""
displayNameResult.TextColor3 = Color3.fromRGB(255, 200, 100)
displayNameResult.TextSize = 11
displayNameResult.Font = Enum.Font.GothamBold
displayNameResult.TextXAlignment = Enum.TextXAlignment.Left
displayNameResult.Parent = mainFrame

local buttonContainer = Instance.new("Frame")
buttonContainer.Size = UDim2.new(0.9, 0, 0, 30)
buttonContainer.Position = UDim2.new(0.05, 0, 0, 95)
buttonContainer.BackgroundTransparency = 1
buttonContainer.Parent = mainFrame

local fakeButton = Instance.new("TextButton")
fakeButton.Size = UDim2.new(0.45, -5, 1, 0)
fakeButton.Position = UDim2.new(0, 0, 0, 0)
fakeButton.BackgroundColor3 = Color3.fromRGB(200, 80, 20)
fakeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
fakeButton.TextSize = 11
fakeButton.Font = Enum.Font.GothamBold
fakeButton.Text = "🎯 Fake Log User"
fakeButton.Parent = buttonContainer

local fakeCorner = Instance.new("UICorner")
fakeCorner.CornerRadius = UDim.new(0, 4)
fakeCorner.Parent = fakeButton

local clearButton = Instance.new("TextButton")
clearButton.Size = UDim2.new(0.45, -5, 1, 0)
clearButton.Position = UDim2.new(0.55, 0, 0, 0)
clearButton.BackgroundColor3 = Color3.fromRGB(180, 70, 30)
clearButton.TextColor3 = Color3.fromRGB(255, 255, 255)
clearButton.TextSize = 11
clearButton.Font = Enum.Font.GothamBold
clearButton.Text = "🧹 Clear Chat"
clearButton.Parent = buttonContainer

local clearCorner = Instance.new("UICorner")
clearCorner.CornerRadius = UDim.new(0, 4)
clearCorner.Parent = clearButton

local statusLabel = Instance.new("TextLabel")
statusLabel.Size = UDim2.new(0.9, 0, 0, 18)
statusLabel.Position = UDim2.new(0.05, 0, 0, 132)
statusLabel.BackgroundTransparency = 1
statusLabel.Text = "● Ready"
statusLabel.TextColor3 = Color3.fromRGB(150, 200, 100)
statusLabel.TextSize = 10
statusLabel.Font = Enum.Font.Gotham
statusLabel.TextXAlignment = Enum.TextXAlignment.Left
statusLabel.Parent = mainFrame

local dragging = false
local dragInput, dragStart, startPos

dragBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = mainFrame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

dragBar.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        local delta = input.Position - dragStart
        mainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

inputBox:GetPropertyChangedSignal("Text"):Connect(function()
    local partial = inputBox.Text
    local foundName = ""
    
    if partial ~= "" then
        for _, player in pairs(Players:GetPlayers()) do
            if string.lower(string.sub(player.DisplayName, 1, #partial)) == string.lower(partial) then
                foundName = player.DisplayName
                break
            end
        end
    end
    
    displayNameResult.Text = foundName ~= "" and "✅ " .. foundName or ""
end)

fakeButton.MouseButton1Click:Connect(function()
    local username = displayNameResult.Text:gsub("✅ ", "")
    if username == "" then
        statusLabel.Text = "❌ No valid display name found!"
        statusLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
        return
    end
    
    statusLabel.Text = "⏳ Sending fake logs..."
    statusLabel.TextColor3 = Color3.fromRGB(255, 200, 100)
    
    local messages = {
        username .. " - Grabbing Data…",
        "Cracking LAX IP…",
        username .. " : IP 36.0.089 OS!",
        username .. "'s Data Has Been Successfully Stolen."
    }
    
    for i, msg in ipairs(messages) do
        task.wait(1.2)
        SendMessage(msg)
        statusLabel.Text = "📤 " .. msg
    end
    
    statusLabel.Text = "✅ All messages sent!"
    statusLabel.TextColor3 = Color3.fromRGB(100, 255, 100)
    
    task.wait(2.5)
    statusLabel.Text = "● Ready"
    statusLabel.TextColor3 = Color3.fromRGB(150, 200, 100)
end)

clearButton.MouseButton1Click:Connect(function()
    statusLabel.Text = "⏳ Clearing chat..."
    statusLabel.TextColor3 = Color3.fromRGB(255, 200, 100)
    
    SendMessage("!clear")
    task.wait(0.3)
    SendMessage("/clear")
    
    statusLabel.Text = "✅ Chat cleared!"
    statusLabel.TextColor3 = Color3.fromRGB(100, 255, 100)
    
    task.wait(1.5)
    statusLabel.Text = "● Ready"
    statusLabel.TextColor3 = Color3.fromRGB(150, 200, 100)
end)
